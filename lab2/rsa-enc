#!/usr/bin/env python

from sys import argv,exit
import os.path

def main():
#	if len(argv) != 7:
#		print("Usage: ./rsa-enc -k <key-file> -i <input-file> -o <output-file>")
#		raise Exception("Bad Command Line Args")
	
	fin = open(argv[2],"r")
	vals = fin.read().splitlines()
	fin.close()
	
	numBitsN = vals[0]
	N = vals[1]
	e = vals[2]

	fin.open(argv[4],"r")
	msg = fin.read().strip()	#If not working, might need to get rid of strip(), didn't work when reading bytes
	fin.close()



if __name__ == "__main__":
	exit(main())
